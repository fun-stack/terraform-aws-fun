{
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "esbuild": "^0.11.9",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "build": "tsc -noEmit index.ts && esbuild index.ts --minify --bundle --platform=node --outfile=build/index.js"
  }
}
